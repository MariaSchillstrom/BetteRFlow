# 5. Applikationens komponenter

In med exempel på Brf Models → DbContext → Controller →





#### **Models (Klasser) = Databastabeller**

**Brf.cs** i Shared/Models:

```cs
public class Brf
{
    public int ID { get; set; }
    public string Namn { get; set; }
    public string OrganisationsNummer { get; set; }
    public string Adress { get; set; }
    // ... alla fält
}
```

#### **DbContext = Databas-koppling**

**BetteRFlowContext.cs:**

```cs
public DbSet<Brf> Brfs { get; set; }
```

